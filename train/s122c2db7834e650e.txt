import math

(a, b, C) = map(float, input().split())
C = math.radians(C)
c = math.sqrt(a * a + b * b - 2 * a * b * math.cos(C))

S = a * b * math.sin(C) / 2
L = a + b + c
h = a * math.sin(C)

S = format(S, '.8f')
L = format(L, '.8f')
h = format(h, '.8f')
print(S, L, h, sep = '\n')