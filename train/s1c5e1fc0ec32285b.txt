#include<bits/stdc++.h>
using namespace std;
#define LL long long
#define ULL unsigned long long
#define mp make_pair
#define pb push_back
#define pii pair<int,int>
#define pll pair<LL,LL>
#define x first
#define y second
#define pi acosl(-1)
#define sqr(x) ((x)*(x))
#define pdd pair<double,double>
#define MEMS(x) memset(x,-1,sizeof(x))
#define MEM(x) memset(x,0,sizeof(x))
#define less Less
#define EPS 1e-4
#define arg ARG
#define cpdd const pdd
#define rank Rank
#define MXN 200005
int main(){ 
    int p[200005];
    int n;
    scanf("%d",&n);
    if(n==1){
        printf("0\n");
        return 0;
    }
    for(int i=0;i<n;i++){
        int x;
        scanf("%d",&x);
        p[x]=i;
    }
    int ans=0;
    int last=0;
    if(p[n-1]>p[n])ans++,last=n;
    else last=n-1;
    for(int i=n-2;i>=1;i--){
        if(p[i]<p[i+1]&&(p[last]<p[i]||p[last]>p[i+1])){
          //  if(p[last]>p[i+1])
        //    last=i;
        }
        else if(p[i]>p[i+1]&&(p[i]>p[last]&&p[last]>p[i+1])){
            //last=i+1;
        }
        else{
            ans++;
            last=i+1;
        }
    }
    printf("%d\n",ans);
}
/**/