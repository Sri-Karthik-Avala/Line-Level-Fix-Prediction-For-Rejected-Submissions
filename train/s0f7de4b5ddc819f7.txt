import sys

def solve():
    
    while True:
        try:
            n = input()
        
        except EOFError:
            break
        d = sorted(map(int,raw_input().split()))
        for i in range(1,n):
            d[i] += d[i-1]
        print sum(d)
                            
if __name__ == "__main__":
    solve()
    n = raw_input()