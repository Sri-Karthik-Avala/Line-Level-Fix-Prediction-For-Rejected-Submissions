while True:
    h, w = map(int, input().split())
    if w+h == 0:
        break
    line = ""
    for idx, y in enumerate(h):
        if idx == 0 or idx == h-1:
            line = "#"*w
        else:
            line = "#"+"."*(w-2)+"#"
        print(line)
    print()

