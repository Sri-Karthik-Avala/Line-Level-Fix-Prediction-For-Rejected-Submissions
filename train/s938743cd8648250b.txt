n = int(input())
dp = [float('inf')]*(n+1)
dp[0] = 0
for i in range(1,n):
    cost = int(input())
    for j in range(i):
        dp[j] = min(dp[j],dp[i-j]+cost)
        dp[i-j] = min(dp[j]+cost,dp[i-j])
    #print(dp)
print(dp[n//2])