#include<iostream>
using namespace std;

int main(){
    int time, h, m,s;
    
    cin >> time;
    h = time / 3600;
    m = (time - h * 3600) / 60;
    s = time - 3600 * h + 60 * m;
    
    cout << h << ":" << m << ":" << s << endl;
    
    return 0;
}
