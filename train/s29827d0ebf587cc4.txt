#include <bits/stdc++.h>
using namespace std;
int n,t,a[200001];
long long ans;
int main() {
    cin>>n>>t;
    for (int i=0; i<n; i++) cin>>a[i];
    for (int i=1; i<n; i++) {
        if (a[i]>t) ans+=t;
        else ans+=a[i];
    }   
    ans+=t;
    cout<<ans<<'\n';
}