#! /usr/bin/python
# -*- coding: utf-8 -*-

#LPABCDEFSM
#0123456789

while 1:
    N = int(raw_input())
    if N==0: break
    crop = [raw_input().split() for i in range(N)]
    for i in range(N): crop[i][1:] = map(int, crop[i][1:])
    ef = []
    for i in range(N):
        com = (crop[i][-3]*crop[i][-1])*crop[i][-2]-crop[i][1]
        t = sum(crop[i][1:7])
        for j in range(crop[i][-1]-1):
            t += sum(crop[i][5:7])
        ef.append([-com/float(t), crop[i][0]])
    ef.sort()
    for i in range(N): print ef[i][1]
    print '#'
        