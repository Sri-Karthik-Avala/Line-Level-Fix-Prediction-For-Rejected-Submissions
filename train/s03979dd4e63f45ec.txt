#include<iostream>
#include<algorithm>
#include<vector>
#include<queue>
#include<map>
#include<set>
#include<numeric>
#include<math.h>
using namespace std;
typedef long long ll;
ll mod = 1e9+7;
#define rep(i,n) for(int i = 0; i < (n); ++i)
int main() {
    int n, k;
    ll s;
    cin >> n >> k >> s;
    int cnt = 0;
    rep(i, n) {
        if ( i != 0 ) cout << " ";
        ll out_num = s + 1;
        if ( cnt >= k ) {
            cout << out_num;
        }
        else {
            cout << s;
        }
        ++cnt;
    }
    cout << endl;
    return 0;
}
