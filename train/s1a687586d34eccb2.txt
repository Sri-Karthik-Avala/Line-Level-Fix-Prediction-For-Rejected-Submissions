#include <bits/stdc++.h>
using namespace std;
int n,a[100],s;
int main(){
  cin>>n;
  for(int i=0;i<2*n;++i)cin>>a[i];
  sort(a,a+2*n);
  for(int i=0;i<n;++i)s+=a[i*2];
  cout << s << endl;
  return 0;
}