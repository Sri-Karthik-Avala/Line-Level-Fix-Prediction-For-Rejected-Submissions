#! /usr/bin/env python
# -*- coding: utf-8 -*-

import os
import sys
import itertools
import math
from collections import Counter, defaultdict

class Main(object):
    
    def __init__(self):
        pass

    def solve(self):
        '''
        insert your code
        '''
        while True:
            x, y, s = map(int, raw_input().split())
            if x == 0 and y == 0 and s == 0:
                break
            before = [None] * 1001
            after = [None] * 1001
            for i in range(1, 1001):
                before[i] = i * (100 + x) / 100
                after[i] = i * (100 + y) / 100
            ans = -1
            for i in range(1, s):
                for j in range(i+1, s):
                    if before[i] + before[j] == s and ans < after[i] + after[j]:
                        ans = after[i] + after[j]
            
            # for a in range(1, 1001):
            #     b = 1001 - a
            #     if before[a] + before[b] == s and ans < after[a] + after[b]:
            #         ans = after[a] + after[b]
            print ans
        return None

if __name__ == '__main__':
    m = Main()
    m.solve()