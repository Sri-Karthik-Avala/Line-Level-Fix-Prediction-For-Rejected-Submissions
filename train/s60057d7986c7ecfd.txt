#include <cstdio>
#include <cstring>
using namespace std;

int main()
{
	int num;
	char str[100][20];
	
	scanf("%d", &num);
	
	for (int i = 0; i < num; i++){
		scanf("%s", str[i]);
		if(strcmp(str[0], str[i]) > 0){
			strcpy(str[0], str[i]);
		}
	}
	
	printf("%s\n", str[0]);
	
	return(0);
}