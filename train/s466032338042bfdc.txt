#include<iostream>

using namespace std;

#define rep(i, n) for (int i = 0; i < int(n); ++i)

int main() {
  string str;
  cin >> str;
  rep (i, str.size()) {
    cout << ">";
    rep (j, str[i]) cout << "+";
    cout << ".";
  }
}