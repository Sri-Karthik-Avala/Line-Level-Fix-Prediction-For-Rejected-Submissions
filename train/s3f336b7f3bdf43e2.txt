#include <cstdio>
#include <iostream>
#include <cstdio>
#include <cassert>
#include <algorithm>
#include <functional>
#include <iomanip>
#include<string>
#include <numeric>
using namespace std;
int a[810];
#define rep(i,n) for(int i=0;i<n;i++)
int main() {
	int n;
	cin >> n;
	int x = 0;
	rep(i, n) { cin >> a[i]; x++; }
	if (x % 2 != 0) { rep(i, n) { if (a[i] % 2 != 0) { cout << (x - a[i])/2 << endl; return 0; } } }
	else { cout << x /2<< endl; }
}


