#include<iostream>
#include <algorithm>
using namespace std;
int N, A[1000000+10];
int main(){
  cin >> N;
/*
  for (int i=0; i<N; i++) cin >> A[i];
  int min;
  int buf;
  for (int i=0; i<N; i++){
    min = i;
    for (int j=i; j<N; j++){
      if (A[j] < A[min])
	min = j;
    }
    buf = A[i];
    A[i] = A[min];
    A[min] = buf;
  }
*/
  sort(A, A+N);
  for (int i=0; i<N; i++) cout << (i?" ":"") << A[i];
  cout << endl;
}