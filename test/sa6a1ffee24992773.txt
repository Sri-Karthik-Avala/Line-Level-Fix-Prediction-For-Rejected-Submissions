
//http://judge.u-aizu.ac.jp/onlinejudge/description.jsp?id=2340

#include<iostream>
#include<map>
#include<vector>
#include<algorithm>
#include<cmath>
#include<climits>
#include<ctime>
#include<cstring>
#include<numeric>

#define ALL(v) (v).begin(),(v).end()
#define REP(i,p,n) for(int i=p;i<(int)(n);++i)
#define rep(i,n) REP(i,0,n)
#define dump(a) (cerr << #a << "=" << (a) << endl)
#define DUMP(list) cout << "{ "; for(auto nth : list){ cout << nth << " "; } cout << "}" << endl;

using namespace std;

int main()
{
	int Q;
	cin >> Q;

	int P,N;
	char C;
	int LPAR=0, RPAR=0;
	rep(nth,Q)
	{
		cin >> P >> C >> N;
		
		if(C == '('){
			LPAR += N;
		}else{
			RPAR += N;
		}

		cout << ( (LPAR == RPAR) ? "Yes" : "No" ) << endl;
	}
	return 0;
}